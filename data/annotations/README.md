# Annotation Summary

`annotation_summary.csv` is an aggregate export from the annotators' results.

It contains per-item consensus ratings for the human evaluation rubric; individual annotator columns omitted.

`PIE-V_Annotation_Template.xlsx` is the public annotation template used to structure the rubric fields.
