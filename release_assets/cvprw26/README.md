# cvprw26 Release Assets

This folder records the MP4 filenames used by the `cvprw26` GitHub release and
the project page.

The MP4 files are ignored by git. `manifest.json` contains the public asset names,
take names, durations, and clip windows. Lightweight GIF previews are stored in
`docs/assets/` for GitHub Pages.
